import { Component } from '@angular/core';

@Component({
    moduleId: module.id,
    selector: 'app',
    template: `
              <h1>My Angular 2 Directives</h1>
              <router-outlet></router-outlet>`
})

export class AppComponent {}
